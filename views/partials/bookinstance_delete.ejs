<h1><%= title %> : ID - <%= bookinstance._id %></h1>
<p>
  <strong>Title:</strong>
  <a href="<%= bookinstance.book.url %>"><%= bookinstance.book.title %></a>
</p>
<p>
  <strong>Status:</strong>
  <span
    class="<%= bookinstance.status === 'Available' ? 'text-success' : 'text-danger' %>"
    ><%= bookinstance.status %></span
  >
</p>
<% if (bookinstance.status !== 'Available') { %>
<p>
  <strong>Status must be 'Available' to delete this book instance.</strong>
</p>
<% } else { %>
<!-- In a full implementation, the control would be made visible only to authorized users.-->
<p>Do you really want to delete this Book Instance?</p>
<form action="" method="post">
  <div class="form-group">
    <input
      type="hidden"
      id="bookinstanceid"
      class="form-control"
      name="bookinstanceid"
      value="<%= bookinstance._id %>"
      required
    />
  </div>
  <button type="submit" class="btn btn-primary">Delete</button>
</form>
<% } %>
